"""上位机名称及发布版本的唯一来源，每次正式功能调整只递增一次，重复打包不递增。"""

APP_NAME = "DFDL33_SH8619协议转换单元上位机"  # 上位机名称独立于单片机固件名称和版本。
APP_VERSION = "1.0.1"  # 当前正式版本；下一次调整发布时递增为1.0.2。
EXE_NAME = f"{APP_NAME}_V{APP_VERSION}"  # 打包文件名统一由名称和版本生成，不含.exe扩展名。
